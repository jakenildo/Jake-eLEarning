<% if logged_in? %>
<nav class="navbar navbar-light navbar-expand-md navigation-clean-button">
    <div class="container"><a class="navbar-brand" href="#">Word Choice</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse"
            id="navcol-1">
            <ul class="nav navbar-nav mr-auto"></ul>
            <span class="navbar-text actions">
                <a class="login" href="#"><i class="fa fa-home"></i> Dashboard</a>
            </span>
            <span class="navbar-text actions">
                <a class="login" href="#"><i class="fa fa-book"></i> Lessons</a>
            </span>
            <span class="navbar-text actions">
                <%= link_to raw("<i class='fa fa-users'></i>  Members"), members_path, class: "login" %>
            </span>
            <% if current_user.account_type == 1 %>
            <span class="navbar-text actions">
                <%= link_to raw("<i class='fa fa-user-tie'></i>  Admin"), lessons_path, class: "login" %>
            </span>
            <% end %>
                <div class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false" href="#"><i class="fa fa-user-circle"></i> Profile</a>
                    <div class="dropdown-menu" role="menu">
                    <%= link_to raw("<i class='fa fa-user-circle'></i>  Profile"), current_user, class: "dropdown-item" %>
                    <%= link_to raw("<i class='fa fa-cogs'></i>  Settings"), edit_user_path(current_user), class: "dropdown-item" %>
                    <%= link_to raw("<i class='fa fa-sign-out-alt'></i>  Logout"), logout_path, class: "dropdown-item", method: :delete %>
                </div>
        </div>
    </div>
</nav>
<% else %>
<nav class="navbar navbar-light navbar-expand-md navigation-clean-button">
    <div class="container"><a class="navbar-brand" href="#">Word Choice</a><button data-toggle="collapse" data-target="#navcol-1" class="navbar-toggler"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse"
            id="navcol-1">
            <ul class="nav navbar-nav mr-auto"></ul>
                <span class="navbar-text actions">
                    <ul class="nav navbar-nav mr-auto"></ul>
                        <span class="navbar-text actions">
                            <%= link_to "Signup", signup_path, class: "btn btn-light action-button", role: "button" %>
                            <%= link_to "Login", login_path, class: "login" %>
                        </span>
                    </div>
                </span>
            </div>
    </div>
</nav>
<% end %>