<%= form_for(current_user.active_relationships.find_by(followed_id: @users.ids),
            html: { method: :delete },
            remote: true) do |f|  %>
    <%= f.submit "Unfollow", class: "btn btn-large btn-danger" %>
<% end %>